//! AI Health Analyst (Phase 10): tool implementations and the agent loop
//! live here in later issues (10.2's `tools.rs`, 10.3's `loop.rs`). This
//! module currently holds only [`embedder`] — Issue 10.1's research spike
//! output, a prerequisite for Issue 10.4's `search_similar_incidents`.

pub mod embedder;
