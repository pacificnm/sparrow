/target

# Tauri's auto-generated schema output (regenerated by tauri_build::build()
# on every build; capabilities/default.json's $schema reference is the only
# thing pointing at it).
/desktop/src-tauri/gen/
